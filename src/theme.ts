import { createTheme } from '@mui/material';

const theme = createTheme({
  typography: {
    button: {
      textTransform: 'none',
      width: 'auto',
      height: 'auto'
    }
  }
});

export default theme;
